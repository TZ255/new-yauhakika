<div class="section-stack">
  <section class="section-card auth-card">
    <div class="section-header">
      <div>
        <h1 class="section-title h4 mb-1">Weka Upya Nenosiri</h1>
      </div>
    </div>

    <p class="text-muted mb-4">
      Jaza email yako ili kutumiwa OTP. Utapokea OTP ya kuthibitisha
    </p>

    <div class="mb-4">
      <h2 class="h6 fw-bold mb-2">1. Pokea OTP</h2>
      <%- include('../fragments/alert', { id: 'reset-request-alert', type: 'danger', message: '' }) %>
      <form
        id="reset-request-form"
        class="mt-3 auth-hx-form"
        hx-post="/auth/reset/request"
        hx-target="#reset-request-alert"
        hx-swap="outerHTML"
        hx-indicator="#reset-request-spinner"
      >
        <div class="mb-3">
          <label for="reset-email" class="form-label">Email</label>
          <input type="email" class="form-control" id="reset-email" name="email" required />
          <div class="form-text">Inaruhusiwa: Gmail, iCloud, Yahoo, Outlook.</div>
        </div>
        <button type="submit" class="btn btn-outline-success d-flex align-items-center gap-2">
          Tuma OTP
          <span
            id="reset-request-spinner"
            class="spinner-border spinner-border-sm htmx-indicator"
            role="status"
            aria-hidden="true"
          ></span>
        </button>
      </form>
    </div>

    <hr class="my-4" />

    <div>
      <h2 class="h6 fw-bold mb-2">2. Thibitisha OTP na nenosiri jipya</h2>
      <%- include('../fragments/alert', { id: 'reset-confirm-alert', type: 'danger', message: '' }) %>
      <form
        id="reset-confirm-form"
        class="mt-3 auth-hx-form"
        hx-post="/auth/reset/confirm"
        hx-target="#reset-confirm-alert"
        hx-swap="outerHTML"
        hx-indicator="#reset-confirm-spinner"
      >
        <div class="mb-3">
          <label for="confirm-email" class="form-label">Email</label>
          <input type="email" class="form-control" id="confirm-email" name="email" required />
        </div>
        <div class="mb-3">
          <label for="reset-code" class="form-label">OTP (tarakimu 4)</label>
          <input
            type="text"
            class="form-control"
            id="reset-code"
            name="code"
            required
            pattern="\d{4}"
            maxlength="4"
            inputmode="numeric"
          />
          <div class="form-text">OTP inaisha muda wake baada ya masaa 4</div>
        </div>
        <div class="mb-4">
          <label for="new-password" class="form-label">Nenosiri jipya</label>
          <input type="password" class="form-control" id="new-password" name="password" required minlength="4" />
        </div>
        <button type="submit" class="btn btn-success w-100 d-flex align-items-center justify-content-center gap-2">
          Thibitisha
          <span
            id="reset-confirm-spinner"
            class="spinner-border spinner-border-sm htmx-indicator"
            role="status"
            aria-hidden="true"
          ></span>
        </button>
      </form>
    </div>
  </section>
</div>
