<!DOCTYPE html>
<html lang="sw">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Maelekezo ya Malipo</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
    integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
    crossorigin="anonymous">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css"
    integrity="sha512-DTOQO9RWCH3ppGqcWaEA1BIZOC6xxalwEsw9c2QQeAIftl+Vegovlnee1c9QX4TctnWMn13TZye+giMm8e2LwA=="
    crossorigin="anonymous" referrerpolicy="no-referrer" />
  <style>
    :root {
      --accent: #0f9d58;
      --bg: #f4f6f8;
      --card: #ffffff;
      --text: #1f2933;
    }

    * {
      box-sizing: border-box;
    }

    body {
      margin: 0;
      padding: 0;
      background: radial-gradient(circle at 10% 10%, #e6f4ea 0, transparent 25%), radial-gradient(circle at 90% 20%, #e0f2fe 0, transparent 22%), var(--bg);
      font-family: 'Inter', 'Segoe UI', system-ui, -apple-system, sans-serif;
      color: var(--text);
    }

    .wrapper {
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 24px;
    }

    .card {
      width: min(540px, 100%);
      background: var(--card);
      border-radius: 18px;
      box-shadow: 0 12px 35px rgba(0, 0, 0, 0.08);
      overflow: hidden;
      border: 1px solid #e5e7eb;
      animation: fadeIn 0.35s ease-out;
    }

    .card__header {
      padding: 18px 20px;
      background: linear-gradient(135deg, var(--accent), #0b7f47);
      color: #fff;
    }

    .card__header h1 {
      margin: 0;
      font-size: 1.35rem;
      font-weight: 700;
    }

    .card__header p {
      margin: 6px 0 0;
      opacity: 0.9;
      font-size: 0.95rem;
    }

    .card__body {
      padding: 20px;
    }

    .summary {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 12px;
      margin-bottom: 14px;
    }

    .pill {
      padding: 12px;
      border-radius: 12px;
      background: #f8fafc;
      border: 1px solid #e5e7eb;
    }

    .pill__label {
      display: block;
      font-size: 0.8rem;
      color: #6b7280;
      margin-bottom: 4px;
    }

    .pill__value {
      font-weight: 700;
      display: flex;
      align-items: center;
      gap: 8px;
    }

    .steps {
      background: #f9fffb;
      border: 1px solid #d1fae5;
      border-radius: 12px;
      padding: 14px 4px;
      margin-top: 10px;
    }

    .footer-note {
      margin-top: 14px;
      font-size: 0.9rem;
      color: #4b5563;
      display: flex;
      align-items: center;
      gap: 8px;
    }

    .cta-row {
      display: flex;
      flex-wrap: wrap;
      gap: 10px;
      margin-top: 24px;
    }

    .cta-row .btn {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      gap: 8px;
      padding: 12px 16px;
      border-radius: 12px;
      border: none;
      font-weight: 700;
      cursor: pointer;
      text-decoration: none;
    }

    .btn-primary {
      background: linear-gradient(135deg, var(--accent), #0b7f47);
      color: #fff;
      box-shadow: 0 10px 25px rgba(15, 157, 88, 0.3);
    }

    .btn-ghost {
      background: #f8fafc;
      color: #0b7f47;
      border: 1px solid #d1fae5;
    }

    @media (max-width: 640px) {
      .summary {
        grid-template-columns: 1fr;
      }

      .wrapper {
        padding: 16px;
      }
    }

    @keyframes fadeIn {
      from {
        opacity: 0;
        transform: translateY(12px);
      }

      to {
        opacity: 1;
        transform: translateY(0);
      }
    }
  </style>
</head>

<body>
  <% const supportEmail = 'uhakikatips@gmail.com'; %>
  <% const mailSubject = `VIP Payment - ${phone}`; %>
  <% const mailBody = `Habari, malipo yangu hayajathibitishwa bado. Tafadhali angalia.\\n\\nPayment ID: ${paymentId}\\nPhone: ${phone}`; %>
  <% const mailToLink = `mailto:${supportEmail}?subject=${encodeURIComponent(mailSubject)}&body=${encodeURIComponent(mailBody)}`; %>
  <div class="wrapper">
    <div class="card">
      <div class="card__header">
        <h1><i class="fa-solid fa-wallet me-2"></i>Malipo ya VIP</h1>
        <p>Maelekezo kulipia kupitia <strong><%= network?.label %></strong></p>
      </div>
      <div class="card__body">
        <div class="summary">
          <div class="pill">
            <span class="pill__label">Namba Yako ya Simu</span>
            <span class="pill__value">
              <i class="fa-solid fa-mobile-screen"></i>
              <%= phone %>
            </span>
          </div>
          <div class="pill">
            <span class="pill__label">Kiasi cha Kulipa</span>
            <span class="pill__value">
              <i class="fa-solid fa-coins text-warning"></i>
              TSh. <%= amount %>
            </span>
          </div>
        </div>

        <div class="pill mb-3">
          <span class="pill__label" style="font-size: .70rem;">Lipa Namba ya Tigo (inapokea mitandao yote)</span>
          <span class="pill__value">
            <i class="fa-solid fa-hashtag text-success"></i>
            <%= network?.payCode %>
          </span>
          <span class="text-muted fw-normal"><i class="fa-regular fa-user text-success me-2"></i><%= network?.payName %></span>
        </div>

        <div class="steps">
          <%- include(`../fragments/lipa-instructions/${network.template}`); %>
        </div>

        <div class="footer-note">
          <i class="fa-solid fa-circle-info text-success"></i>
          <span>
            Mara nyingi uthibitisho huchukua dakika chache, ikitokea kuna ucheleweshaji, tafadhali tuandikie
            <a href="<%= mailToLink %>" class="link-success">uhakikatips@gmail.com</a>
          </span>
        </div>

        <div class="cta-row">
          <a class="btn btn-primary" href="/vip"><i class="fa-solid fa-arrow-left"></i> Rudi kwenye VIP</a>
          <button class="btn btn-ghost" onclick="window.print()"><i class="fa-solid fa-print"></i> Print</button>
        </div>
      </div>
    </div>
  </div>
</body>
</html>
