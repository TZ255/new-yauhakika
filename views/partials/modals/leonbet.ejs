<div class="modal fade" id="leonbetModal" tabindex="-1" aria-labelledby="leonbetModalLabel" aria-hidden="true">
  <div class="modal-dialog modal-dialog-centered">
    <div class="modal-content" style="background-color:#fff5f5;border-radius:12px;overflow:hidden;box-shadow:0 15px 35px rgba(220,38,38,0.25);border:2px solid #fecaca;">
      <div class="modal-header py-2" style="background:linear-gradient(135deg,#dc2626,#16a34a);color:#ffffff;">
        <h5 class="modal-title fw-bold d-flex align-items-center gap-2 mb-0" id="leonbetModalLabel">
          <span style="text-shadow:2px 2px 4px rgba(0,0,0,0.3);">10% CASHBACK</span>
          <span aria-hidden="true">🔥🔥</span>
        </h5>
        <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close" style="filter:invert(1);"></button>
      </div>
      <div class="modal-body py-3" style="background:linear-gradient(180deg,#fff5f5,#f0fdf4);">
        <p class="mb-3 fw-semibold" style="color:#1f2937;font-size:0.90rem;">
          Tunakurudishia asilimia 10 ya dau lako kwa kila mkeka utakaochanika 🔥
        </p>
        <div class="rounded-3 shadow-sm mb-2" style="cursor:pointer;box-shadow:0 6px 18px rgba(220,38,38,0.2);border:3px solid #16a34a;" onclick="OpenAff('leonbet')">
          <img src="/ads/leonbet-10-cashback.webp" alt="Leonbet Tanzania - 10% CASHBACK" class="img-fluid rounded-3 w-100" />
        </div>
      </div>
      <div class="modal-footer w-100 d-flex justify-content-between align-items-center py-2" style="background:linear-gradient(90deg,#fef2f2,#dcfce7);border-top:2px solid #16a34a;">
        <div class="d-flex flex-column" style="color:#166534;">
          <strong style="color:#dc2626;font-size:0.95rem;">Ofa inakusubiri!</strong>
          <small style="color:#15803d; font-size: 0.75rem;">Jisajili Simba la Mikeka</small>
        </div>
        <button type="button" class="btn fw-bold text-white" style="background:linear-gradient(135deg,#dc2626,#16a34a);border:none;border-radius:999px;padding:0.45rem 1.4rem;box-shadow:0 4px 12px rgba(220,38,38,0.4);font-size:0.95rem;" onclick="OpenAff('leonbet')">
          JISAJILI SASA
        </button>
      </div>
    </div>
  </div>
</div>
<script>
  document.addEventListener('DOMContentLoaded', function () {
    const modalElement = document.getElementById('leonbetModal');
    if (!modalElement || !window.bootstrap) return;
    const leonbetModal = new bootstrap.Modal(modalElement);
    let showTimer;
    const scheduleShow = (delay) => {
      clearTimeout(showTimer);
      showTimer = setTimeout(() => leonbetModal.show(), delay);
    };
    scheduleShow(15000);
    modalElement.addEventListener('hidden.bs.modal', () => {
      scheduleShow(30000);
    });
  });
</script>