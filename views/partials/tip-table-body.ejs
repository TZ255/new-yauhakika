<% if (!tips || tips.length === 0) { %>
  <div class="tip-row empty">Hakuna mikeka kwa siku hii. Tafadhali rudi baadae.</div>
<% } %>
<% (tips || []).forEach((tip) => { %>
  <div class="tip-row">
    <div class="tip-cell time">
      <div class="fw-semibold"><%= tip.kickoff || 'TBD' %></div>
      <div class="small text-muted"><%= tip.dateLabel || '' %></div>
    </div>
    <div class="tip-cell match">
      <div class="league-chip"><i class="fa-solid fa-trophy"></i> <%= tip.league || 'League TBD' %></div>
      <div class="fw-semibold fs-6 mt-1"><i class="fa-regular fa-futbol"></i> <%= tip.match %></div>
      <% if (tip.explanation) { %>
        <div class="tip-note"><%= tip.explanation %></div>
      <% } %>
    </div>
    <div class="tip-cell pick text-center">
      <span class="pill <%= typeof category !== undefined && category === 'vip' ? 'pill-vip' : '' %>"><%= tip.prediction %></span>
    </div>
  </div>
<% }) %>
