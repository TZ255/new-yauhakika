<div class="modal fade" id="betwinnerModal" tabindex="-1" aria-labelledby="betwinnerModalLabel" aria-hidden="true">
  <div class="modal-dialog modal-dialog-centered">
    <div class="modal-content" style="background-color:#f7fee7;border-radius:12px;overflow:hidden;box-shadow:0 15px 35px rgba(0,0,0,0.18);">
      <div class="modal-header py-3" style="background:linear-gradient(90deg,#365314,#166534);color:#ffffff;">
        <h5 class="modal-title fw-bold d-flex align-items-center gap-2 mb-0" id="betwinnerModalLabel">
          <span>100% BONUS</span>
          <span aria-hidden="true">🔥🔥🔥</span>
        </h5>
        <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close" style="filter:invert(1);"></button>
      </div>

      <div class="modal-body">
        <p class="mb-4" style="color:#374151;">
          Pata nafasi ya kushinda zaidi. Weka tukuweke dabo!
        </p>

        <div class="rounded-3 shadow-sm mb-3" style="cursor:pointer;box-shadow:0 6px 18px rgba(0,0,0,0.16);" onclick="OpenAff('betwinner')">
          <img src="/ads/betwinner-short.webp" alt="betwinner Tanzania - 100% BONUS" class="img-fluid rounded-3 w-100" />
        </div>
      </div>

      <div class="modal-footer w-100 d-flex justify-content-between align-items-center" style="background-color:#fef9c3;">
        <div class="d-flex flex-column" style="color:#365314;">
          <strong>Tayari kushinda?</strong>
          <small>Jisajili na mabingwa</small>
        </div>
        <button type="button" class="btn fw-bold text-white" style="background-color:#365314;border-color:#365314;border-radius:999px;padding:0.5rem 1.5rem;" onclick="OpenAff('betwinner')">
          JISAJILI SASA
        </button>
      </div>
    </div>
  </div>
</div>

<script>
  document.addEventListener('DOMContentLoaded', function () {
    const modalElement = document.getElementById('betwinnerModal');
    if (!modalElement || !window.bootstrap) return;

    const betwinnerModal = new bootstrap.Modal(modalElement);
    let showTimer;

    const scheduleShow = (delay) => {
      clearTimeout(showTimer);
      showTimer = setTimeout(() => betwinnerModal.show(), delay);
    };

    scheduleShow(15000);

    modalElement.addEventListener('hidden.bs.modal', () => {
      scheduleShow(30000);
    });
  });
</script>
