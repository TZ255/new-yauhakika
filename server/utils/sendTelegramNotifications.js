export async function sendTelegramNotification(message, notifyAdmins = false) {
  // Placeholder hook; integrate real Telegram bot here if needed.
  console.log('[telegram]', notifyAdmins ? '(admin)' : '', message);
}
